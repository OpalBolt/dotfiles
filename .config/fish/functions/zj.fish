function zj --description "fuzzy-pick a zellij session or layout"
    ~/.config/zellij/zj.sh $argv
end
# ponytail: path assumes zj.sh stays stowed at ~/.config/zellij/ — update this one line after moving it
